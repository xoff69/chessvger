<configuration>

    <!-- Appender Console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender Fichier -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>data/logs/backoffice.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Désactiver totalement les logs HikariConfig -->
    <logger name="com.zaxxer.hikari.HikariConfig" level="OFF" additivity="false" />
    <logger name="com.zaxxer.hikari" level="OFF" additivity="false" />

    <!-- Root Logger -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
